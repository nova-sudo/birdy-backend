fastapi==0.115.0
starlette>=0.37.2,<0.39.0
uvicorn==0.35.0
motor==3.5.1
pymongo==4.8.0
python-dotenv==1.1.0
httpx==0.28.1
bcrypt==4.2.0
PyJWT[crypto]==2.10.1
apscheduler==3.10.4
CurrencyConverter==0.18.15
# Pinned to 0.x deliberately. whop-sdk 1.x is not an upgrade of this library,
# it is a different one: a Fern-generated client taking `token=` instead of
# `api_key=`, with different resources, pagination and exception types. Left
# unpinned, a Vercel rebuild silently moved production onto it, and every call
# in billing.py — promo codes, and the membership lookup behind the billing
# portal URL — started raising
#     AsyncWhop.__init__() got an unexpected keyword argument 'api_key'
# which the old blanket except swallowed as "Could not reach Whop".
#
# Moving to 1.x means rewriting every Whop call in billing.py and re-verifying
# memberships, promo codes and the webhook path against it. Until that is done
# deliberately, pin the version this code is written against.
whop-sdk[webhooks]==0.0.41
groq
anthropic
openai
cryptography
fastmcp==2.14.7
mcp==1.24.0
slack-sdk
aiohttp