fastapi==0.115.0
starlette>=0.37.2,<0.39.0
uvicorn==0.35.0
motor==3.5.1
pymongo==4.8.0
python-dotenv==1.1.0
httpx==0.28.1
bcrypt==4.2.0
PyJWT[crypto]==2.10.1
apscheduler==3.10.4
CurrencyConverter==0.18.15
# Pinned: an unpinned SDK let a Vercel build silently move to 1.x while this
# checkout ran 0.0.41, and 1.x dropped the top-level error exports billing.py
# imported — which failed at module load and took every route down with it.
whop-sdk[webhooks]==1.0.14
groq
anthropic
openai
cryptography
fastmcp==2.14.7
mcp==1.24.0
slack-sdk
aiohttp